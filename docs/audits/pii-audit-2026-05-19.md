# PII / Secrets Audit — SSHGate v1

**Date:** 2026-05-19
**Tool:** `~/arogara/pii-audit/scan.sh` (gitleaks 8.30.0 + wordlist grep)
**Raw report:** `~/arogara/pii-audit/reports/SSHGate-2026-05-19.md`

## Summary

- **gitleaks (working tree):** clean, no findings
- **gitleaks (full history --all):** clean, no findings
- **Wordlist hits:** all reviewed; triaged below

## Wordlist triage

### MUST FIX (fixed in this commit)

1. **Karthi's real Telegram user_id (`12345678`) in test files.** The plan explicitly flagged this as the primary risk. Found and scrubbed in:
   - `src/signer/backend/chatstore_test.go` — replaced literal `12345678` with `99999999`
   - `src/signer/backend/telegram_test.go` — replaced `const allowedUserID = int64(12345678)` with `int64(77777777)` plus a comment noting these are publish-safe synthetic test values
2. **Personal username in classifier corpus:** `READ\tgroups karthi` → `READ\tgroups testuser`. The corpus is committed test data and should be generic.

After these fixes, no instance of `12345678` remains in `src/` or `tests/` (verified by `grep -rn 12345678 src/ tests/` returning empty).

### REVIEW — kept intentionally (documented as Karthi-specific context)

These are all in documentation files that explicitly document Karthi's own deployment of SSHGate. They are NOT runtime secrets and NOT a security risk for the personal-tool stage of the project. For marketplace publish (v1.x roadmap), these would be genericized in a publish-cleanup pass.

- `karthi` in `commands/setup.md` (line 7, "walking the user (Karthi) through ...") — body of the slash command refers to the operator
- `karthi@example.com` in `commands/setup.md` (line 286) — example invocation in the setup command's docstring; `example.com` is the canonical illustrative TLD
- `karthi` and `12345678` in `docs/specs/2026-05-19-sshgate-design.md` — the design spec WAS written for Karthi's deployment; the user_id `12345678` is illustrative of how the from.id check works in his concrete case
- `karthi` and `12345678` in `docs/plans/2026-05-19-sshgate-v1-implementation.md` — same as spec
- `karthi` in `docs/decisions/MORNING-REVIEW-2026-05-19.md` — overnight session log; Karthi's name is the only correct reference
- `karthi` in `docs/install-step-by-step.md` — install guide refers to the operator; uses `karthi` as the running example (could be templated to `<your-user>` for publish)
- `arogara` and `example.com` in spec example URLs — illustrative domains in command examples; `example.com` is the better choice for publish

### FALSE POSITIVE (no action)

- `karthikeyan5` in `go.mod` and Go import paths — this is the Go module path; matches the GitHub username SSHGate would publish under
- `karthi` import lines (`tests/integration/helpers_test.go:37` etc.) — Go imports from `github.com/karthikeyan5/...`; the wordlist matches the path prefix
- `karthi` in test fixtures and call data (`"karthi"` string in `pushCallback` calls) — used as test user's Telegram username; replacing across-the-board makes test data less readable. The value is a string label, not authentication.

## Publish-readiness checklist (for v1.x or v2)

When SSHGate gets prepared for the marketplace, a follow-up pass should:
- Genericize Karthi-specific references in docs (mostly find-and-replace to `<your-user>` and templated examples)
- Change example domains (`example.com` → `example.com`) in spec
- Strip the morning-review file (it's a personal session log)
- Verify the `karthikeyan5` GitHub identity is the intended publisher; rename module if not

## Publish status

- **License selected (2026-05-19):** MIT. See top-level `LICENSE` and the README license section.
- The historical docs under `docs/specs/`, `docs/plans/`, `docs/decisions/`, and `docs/audits/` are preserved as session-log records and may still reference the operator (`karthi`) and his Telegram user_id (`12345678`) in illustrative contexts. The runtime code path and user-facing docs do not embed these values.

## Conclusion

**No MUST-FIX findings remain in source.** v1's PII posture is acceptable for personal use. Publish-cleanup is a documented v1.x task, not blocking v1.
